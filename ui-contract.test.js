import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('HTML loads modular runtime and exposes all key accessible dialogs',async()=>{const html=await readFile(new URL('../index.html',import.meta.url),'utf8');assert.match(html,/<script type="module" src="\.\/src\/app\.js"/);for(const id of ['startDialog','pauseDialog','settingsDialog','levelsDialog','victoryDialog','finalDialog','confirmDialog'])assert.match(html,new RegExp(`<dialog[^>]*id="${id}"[^>]*aria-labelledby=`));});
test('settings controls have labels and no external runtime dependency',async()=>{const html=await readFile(new URL('../index.html',import.meta.url),'utf8');for(const id of ['sensitivity','smoothing','deadZone','invertX','invertZ','quality'])assert.match(html,new RegExp(`for="${id}"`));assert.doesNotMatch(html,/<script[^>]+src="https?:/);});
